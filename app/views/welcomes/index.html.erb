<p id="notice"><%= notice %></p>

<h1>Seriously High Fantasy</h1>

<table>
  <thead>
    <tr>
      <th>Path</th>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @welcomes.each do |welcome| %>
      <tr>
        <td><%= link_to welcome.path, public_send(:"#{welcome.path}_path") %></td>
        <td><%= welcome.title %></td>
        <td><%= link_to 'Show', welcome %></td>
        <td><%= link_to 'Edit', edit_welcome_path(welcome) %></td>
        <td><%= link_to 'Destroy', welcome, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New landing page entry', new_welcome_path %>

<h3>Useful Things</h3>
<p>
  <li><%= link_to "story_time Github", "https://github.com/manik1235/story_time" %></li>
  <li><%= link_to "D&D API", "http://dnd5eapi.co" %></li>
</p>
