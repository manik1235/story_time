require('../../../../app/javascript/views/maps/show')

describe('views/maps/show.js', function() {
  describe('feature', function() {
    it('works', function() {
      pending()
    })

    it('logs an error to the console when no suitable element is found', function() {
      pending()
    })
  })
})
